export default class User {
  constructor (id, email, password, userId, userNm) {
    this.id = id
    this.email = email
    this.password = password
    this.userId = userId
    this.userNm = userNm
  }
}
