export default class User {
  constructor (id, userEmailAddr, password, userId, userNm) {
    this.id = id
    this.userEmailAddr = userEmailAddr
    this.password = password
    this.userId = userId
    this.userNm = userNm
  }
}
